package {{ .Package }}

import (
	"github.com/shauryagautam/Astra/pkg/engine/events"
)

// {{ .Name }} represents a {{ .NameLower }} event.
type {{ .Name }} struct {
	events.BaseEvent
	// TODO: Add event payload fields
}

// Name returns the event name.
func (e {{ .Name }}) Name() string {
	return "{{ .NameSnake }}"
}

// Data returns the event data.
func (e {{ .Name }}) Data() any {
	return e
}
