APP_NAME="Astra Todo API"
APP_ENV=development
APP_PORT=8080

# For testing GORM we need a real database url. Using typical local defaults.
DB_URL="host=localhost user=postgres password=postgres dbname=postgres port=5432 sslmode=disable TimeZone=UTC"

REDIS_HOST=localhost
REDIS_PORT=6379
