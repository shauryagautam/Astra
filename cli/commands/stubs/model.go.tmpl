package {{ .Package }}

import (
	"time"

	"gorm.io/gorm"
)

// {{ .Name }} represents a {{ .NameLower }} record.
type {{ .Name }} struct {
	ID        uint           `gorm:"primaryKey" json:"id"`
	{{ range .Fields }}{{ .FieldName }}  {{ .GoType }}  `gorm:"type:{{ .SQLType }}" json:"{{ .JSONName }}"`
	{{ end }}CreatedAt time.Time      `json:"created_at"`
	UpdatedAt time.Time      `json:"updated_at"`
	DeletedAt gorm.DeletedAt `gorm:"index" json:"-"`
}
