package {{ .Package }}

import (
	"context"
	"github.com/astraframework/astra/events"
)

// {{ .Name }} handles the {{ .NameLower }} event.
type {{ .Name }} struct{}

// Handle processes the event.
func (l *{{ .Name }}) Handle(ctx context.Context, e events.Event) error {
	// TODO: Implement listener logic
	return nil
}
